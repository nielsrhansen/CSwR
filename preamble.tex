\usepackage[headheight=14pt, margin=1in]{geometry}
%\usepackage{booktabs}
\usepackage[osf,sc]{mathpazo}
\usepackage{fancyhdr}
% \usepackage{color}
\definecolor{green}{RGB}{70, 116, 60}
\usepackage{sectsty} % To change the Chapter/Section header colors
\partfont{\color{green}}  
\chapterfont{\color{green}}  
\sectionfont{\color{green}}
\subsectionfont{\color{green}}
% Controls box shading of code
\usepackage{framed}
\definecolor{shadecolor}{RGB}{248,248,248}
\renewenvironment{Shaded}{\begin{snugshade}}{\end{snugshade}}

\pagestyle{fancy}
%\renewcommand{\chaptername}{}
\renewcommand{\chaptermark}[1]{\markboth{#1}{}}
\renewcommand{\sectionmark}[1]{\markright{#1}{}}
\fancyhf{}
\fancyhead[LE,RO]{\color{green} \bfseries \scshape \thepage}
\fancyhead[LO]{\color{green} \bfseries \scshape \nouppercase{\rightmark}}
\fancyhead[RE]{
  \if\thechapter0
    \color{green} \bfseries \nouppercase{\leftmark}
  \else
     \color{green} \bfseries Chapter \thechapter. \nouppercase{\leftmark}
  \fi
}
